services:
  frame-count-api:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - LOG_LEVEL=info
    working_dir: /app
    command: npm run dev
    stdin_open: true
    tty: true
